name: CI

on:
  push:
    branches: [main, development]
  pull_request:
    branches: [main, development]
  workflow_dispatch:
    inputs:
      run_e2e:
        description: 'Run E2E Telegram Bot Tests'
        type: boolean
        default: false

env:
  PYTHON_VERSION: "3.11"

jobs:
  lint:
    name: Lint & Format Check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      - name: Install linting tools
        run: |
          python -m pip install --upgrade pip
          pip install ruff

      - name: Run ruff check
        run: ruff check .

      - name: Run ruff format check
        run: ruff format --check .

  test-cvs-repository:
    name: Test CVs Repository
    runs-on: ubuntu-latest
    needs: lint
    defaults:
      run:
        working-directory: packages/cvs-repository
    steps:
      - uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e "../job-agent-platform-contracts[dev]"
          pip install -e ".[dev]"

      - name: Run tests
        run: pytest -v

  test-jobs-repository:
    name: Test Jobs Repository
    runs-on: ubuntu-latest
    needs: lint
    services:
      postgres:
        image: postgres:15
        env:
          POSTGRES_USER: test_user
          POSTGRES_PASSWORD: test_password
          POSTGRES_DB: test_db
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    defaults:
      run:
        working-directory: packages/jobs-repository
    env:
      DATABASE_URL: postgresql://test_user:test_password@localhost:5432/test_db
      POSTGRES_USER: test_user
      POSTGRES_PASSWORD: test_password
      POSTGRES_DB: test_db
      POSTGRES_PORT: 5432
    steps:
      - uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e "../job-agent-platform-contracts[dev]"
          pip install -e "../db-core[dev]"
          pip install -e ".[dev]"

      - name: Run tests
        run: pytest -v

  test-backend:
    name: Test Backend
    runs-on: ubuntu-latest
    needs: lint
    services:
      postgres:
        image: postgres:15
        env:
          POSTGRES_USER: test_user
          POSTGRES_PASSWORD: test_password
          POSTGRES_DB: test_db
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    defaults:
      run:
        working-directory: packages/job-agent-backend
    env:
      DATABASE_URL: postgresql://test_user:test_password@localhost:5432/test_db
      POSTGRES_USER: test_user
      POSTGRES_PASSWORD: test_password
      POSTGRES_DB: test_db
      POSTGRES_PORT: 5432
      OPENAI_API_KEY: test-key
      RABBITMQ_URL: amqp://guest:guest@localhost:5672/
    steps:
      - uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e "../job-agent-platform-contracts[dev]"
          pip install -e "../db-core[dev]"
          pip install -e "../cvs-repository[dev]"
          pip install -e "../jobs-repository[dev]"
          pip install -e "../essay-repository[dev]"
          pip install -e ".[dev]"

      - name: Run tests
        run: pytest -v

  test-telegram-bot:
    name: Test Telegram Bot
    runs-on: ubuntu-latest
    needs: lint
    services:
      postgres:
        image: postgres:15
        env:
          POSTGRES_USER: test_user
          POSTGRES_PASSWORD: test_password
          POSTGRES_DB: test_db
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    defaults:
      run:
        working-directory: packages/telegram_bot
    env:
      DATABASE_URL: postgresql://test_user:test_password@localhost:5432/test_db
      POSTGRES_USER: test_user
      POSTGRES_PASSWORD: test_password
      POSTGRES_DB: test_db
      POSTGRES_PORT: 5432
      OPENAI_API_KEY: test-key
      JOB_AGENT_BOT_TOKEN: test-token
      RABBITMQ_URL: amqp://guest:guest@localhost:5672/
    steps:
      - uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e "../job-agent-platform-contracts[dev]"
          pip install -e "../db-core[dev]"
          pip install -e "../cvs-repository[dev]"
          pip install -e "../jobs-repository[dev]"
          pip install -e "../essay-repository[dev]"
          pip install -e "../job-agent-backend[dev]"
          pip install -e ".[dev]"

      - name: Run tests
        run: pytest -v

  test-telegram-e2e-tests:
    name: Test Telegram E2E Tests Package
    runs-on: ubuntu-latest
    needs: lint
    defaults:
      run:
        working-directory: packages/telegram-e2e-tests
    steps:
      - uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev]"

      - name: Run unit tests
        run: pytest -m "not e2e" -v

  quality-tests:
    name: Quality Tests (Type Checking)
    runs-on: ubuntu-latest
    needs: [test-cvs-repository, test-jobs-repository, test-backend, test-telegram-bot, test-telegram-e2e-tests]
    steps:
      - uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      - name: Install all packages
        run: |
          python -m pip install --upgrade pip
          pip install pytest mypy
          pip install -e "packages/job-agent-platform-contracts[dev]"
          pip install -e "packages/db-core[dev]"
          pip install -e "packages/cvs-repository[dev]"
          pip install -e "packages/jobs-repository[dev]"
          pip install -e "packages/essay-repository[dev]"
          pip install -e "packages/job-agent-backend[dev]"
          pip install -e "packages/telegram_bot[dev]"

      - name: Run quality tests
        run: pytest -m quality -v quality_tests/

  e2e-telegram-bot:
    name: E2E Telegram Bot Tests
    runs-on: ubuntu-latest
    needs: [test-cvs-repository, test-jobs-repository, test-backend, test-telegram-bot, test-telegram-e2e-tests]
    if: github.event_name == 'workflow_dispatch' && inputs.run_e2e
    defaults:
      run:
        working-directory: packages/telegram-e2e-tests
    env:
      TELEGRAM_API_ID: ${{ secrets.TELEGRAM_API_ID }}
      TELEGRAM_API_HASH: ${{ secrets.TELEGRAM_API_HASH }}
      TELEGRAM_QA_BOT_USERNAME: ${{ secrets.TELEGRAM_QA_BOT_USERNAME }}
      TELEGRAM_QA_SESSION_PATH: ${{ secrets.TELEGRAM_QA_SESSION_PATH }}
    steps:
      - uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev]"

      - name: Run E2E tests
        run: pytest -m e2e -v

  docker-startup:
    name: Docker Startup Test
    runs-on: ubuntu-latest
    needs: quality-tests
    steps:
      - name: Free up disk space
        run: |
          sudo rm -rf /usr/share/dotnet
          sudo rm -rf /usr/local/lib/android
          sudo rm -rf /opt/ghc
          sudo rm -rf /opt/hostedtoolcache/CodeQL
          docker system prune -af

      - uses: actions/checkout@v6

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build Docker image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: packages/telegram_bot/Dockerfile
          tags: telegram-bot:test
          load: true
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Test container startup
        run: |
          docker run --rm \
            -e JOB_AGENT_BOT_TOKEN=test-token \
            -e DATABASE_URL=postgresql://test:test@localhost:5432/test \
            -e OPENAI_API_KEY=test-key \
            telegram-bot:test python -m telegram_bot.main --check
